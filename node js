List 5 difference between Browser JS(console) v Nodejs
ans: NODE JS:
      1. Javascript runtime environment.
      2. run Javascript outside the browser with the help of NodeJS.
      3. used on the server-side.
      4. V8 is the Javascript engine inside of node.js that parses and runs Javascript. 
      5. Nodejs is written in C, C++ and Javascript.
    Javascript:
      1.  programming language that is used for writing scripts on the website.
      2.  only be run in the browsers.
      3.  used on the client-side.
      4. run in any browser engine as like JS core in safari and Spidermonkey in Firefox.
      5. It is the upgraded version of ECMA script that uses Chrome’s V8 engine written in C++. 
